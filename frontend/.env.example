# Example environment configuration for the Cloudflare DNS Manager frontend
# Values are embedded at build time. Do not commit secrets.

# Required: Cloudflare API token with Zone:Read and DNS:Edit permissions
CF_API_TOKEN=your_cloudflare_api_token

# Optional: Override the Cloudflare API base URL
# CF_API_BASE=https://api.cloudflare.com/client/v4

# Required: bcrypt hash of the admin password (e.g. generated via `npx bcrypt-cli your-password`)
AUTH_BCRYPT_HASH=$2b$10$exampleexampleexampleexampleexampleexamp

# Required: secret used to sign the session cookie
SESSION_SECRET=replace_with_a_random_string
